package com.cognizant.ormlearn;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class OrmLearnApplicationTests {

    @Test
    void contextLoads() {
        // This test just checks that the Spring application context starts
        // without any errors. If your DB config is wrong, this will fail.
    }
}
